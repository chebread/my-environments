# How to suppose React app

1. yarn create vite NAME
2. cd NAME
3. yarn set version berry
4. yarn install
5. yarn add @yarnpkg/sdks -D
6. yarn dlx @yarnpkg/sdks vscode
7. yarn plugin import typescript